using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ADiff
{
  //public class Diff
  //{
  //  public void ThreeWayDiff(string parent, string left, string right)
  //  {
  //    var engine = new diff_match_patch();
  //    var leftDiffs = engine.diff_main_line(parent, left, DateTime.Now.AddSeconds(20));
  //    var rightDiffs = engine.diff_main_line(parent, right, DateTime.Now.AddSeconds(20));

  //    foreach (var diff in leftDiffs)
  //    {
        
  //    }
  //  }
  //}
}
